#include <stdio.h>
#include <stdlib.h>

int main()
{
   int arr[10];
   int i=1,m=1,n;
   for(;m<=10;m++)
   {
       scanf("%d",&n);
   }

   for(;i<=10;i++)
   {
       int j=1;
       for(;j<=10;j++)

           if(arr[i]==arr[j])

       printf("%d",arr[i]);
   }
    return 0;
}
